// Package api implements the api component: the shared service core plus its two
// transports (the gRPC JobService and the REST/fiber HTTP surface). It is the
// only package that talks to Postgres.
package api
